# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*

# Android build outputs (regenerated via `npx cap sync android` + Gradle)
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/build/
*.iml
.idea/

# Electron-builder raw output (unpacked app, blockmaps). The final installer
# lives in the curated releases/ folder instead, which is not ignored.
release/

# The Windows installer exceeds GitHub's 100MB file limit; publish it as a GitHub Release
# asset instead (see releases/Wolfman-Setup-Windows.exe, built locally via `npm run dist:windows`).
releases/*.exe

# azd local environment state (never committed; secrets live only here locally)
functions/.azure/

!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
