{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "principalId": {
      "value": "${AZURE_PRINCIPAL_ID}"
    },
    "vnetEnabled": {
      "value": "${VNET_ENABLED}"
    },
    "corsAllowedOrigins": {
      "value": "${CORS_ALLOWED_ORIGINS}"
    },
    "azureMapsKey": {
      "value": "${AZURE_MAPS_KEY}"
    },
    "acsSmsConnectionString": {
      "value": "${ACS_SMS_CONNECTION_STRING}"
    },
    "acsSmsFromNumber": {
      "value": "${ACS_SMS_FROM_NUMBER}"
    },
    "wolfmanApiKey": {
      "value": "${WOLFMAN_API_KEY}"
    }
  }
}